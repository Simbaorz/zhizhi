"""Relational metadata owned by the 致知 subscriber application."""

from sqlalchemy.orm import DeclarativeBase


class ZhizhiBase(DeclarativeBase):
    """Declarative base for tables owned by 致知 subscriber packages."""
